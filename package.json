{
	"name": "manual_sdvx_maple",
	"type": "module",
	"private": true,
	"scripts": {
		"fetch-songs": "bun run scripts/fetch-songs.ts",
		"generate-data": "bun run scripts/generate-data.ts",
		"generate-data:dev": "bun run --watch scripts/generate-data.ts"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"cheerio": "^1.0.0"
	}
}
