{
	"name": "manual_sdvx_maple",
	"type": "module",
	"private": true,
	"scripts": {
		"generate-manual-world": "bun run scripts/generate-manual-world.ts",
		"build": "bun build --target=node --packages=external --outdir=dist src/main.ts",
		"dev": "bun build --watch --target=node --packages=external --outdir=dist src/main.ts"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"cheerio": "^1.0.0"
	}
}
